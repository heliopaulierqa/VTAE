# ── Desktop (OpenCVRunner) ──
opencv-python
pyautogui
pillow
numpy
pyperclip          # colar via clipboard em campos de grade Oracle Forms

# ── OCR ──
easyocr            # leitura de texto em screenshots (pip puro — sem instalacao no SO)
torch              # dependencia do EasyOCR
torchvision        # dependencia do EasyOCR

# ── Web (PlaywrightRunner) ──
playwright

# ── Dados de teste ──
Faker              # geração de dados falsos (nome, CPF, email, etc.)

# ── Testes ──
pytest
pytest-cov
pytest-html        # relatório HTML de execução
pytest-metadata
pytest-repeat      # suporta --count= para repetir execucoes (usado por vtae run --repeat N)

# ── YAML ──
pyyaml>=6.0

# ── Utilitários ──
python-dotenv      # leitura de .env (credenciais)

# ── Foco de janela (Oracle Forms) ──
pygetwindow        # controle de foco no Oracle Forms (Windows)